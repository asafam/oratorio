psycopg[binary,pool]>=3.2.0
# Pinned below 2.0: mcp 2.x renames/reshapes the client streamable-http API
# (e.g. streamablehttp_client -> streamable_http_client) and this code was
# only tested against 1.28.1. Bump deliberately, re-verify
# orchestration/receiver/board_client.py and orchestration/mcp/server_http.py
# against the new API before raising this ceiling.
mcp>=1.2.0,<2.0.0
PyYAML>=6.0
httpx>=0.27.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Dev/test
pytest>=8.0.0
